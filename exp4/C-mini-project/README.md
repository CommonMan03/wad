# Exp4-C Mini Project

This folder now has its own duplicate project files:
- `server.js`
- `package.json`
- `.env.example`
- `Procfile`
- `public/index.html`

Domain used: **Education**.

Run:

```bash
cd exp4/C-mini-project
copy .env.example .env
npm install
npm start
```
