# exp1 split (A/B)

- `A-dashboard/` : responsive admin dashboard with Bootstrap cards + sidebar.
- `B-registration/` : registration form with AJAX POST + localStorage and list page.

Run by opening HTML files in browser.
