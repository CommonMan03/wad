# Exp2-C Angular app (Register, Login, Profile)

Self-contained copy of minimum Angular app files is inside `app-files/`:

- `auth.service.ts`
- `auth.guard.ts`
- `pages.ts`
- `app.routes.ts`
- `app.component.ts`

These are duplicate files for exam writing/reference (not shared from another folder).
